<div class="page">
    {{#each albums}}
    <div class="section-div">
        <img class="cover" src="{{images.0.url}}">
        <h3>{{name}}</h3>
        <a class="link" href="/tracks/{{id}}">View tracks</a>
    </div>
    {{/each}}
</div>